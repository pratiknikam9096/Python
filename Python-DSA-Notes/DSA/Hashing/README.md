# Hashing

Definition: Use of hash functions to map values to buckets for O(1) average access.

Why used: Fast lookup, deduplication, frequency counting.

Complexity: Average O(1) for get/set; worst O(n) depending on collisions.

Real-world: Hash table like an indexed filing cabinet.
